# JavaScript: Manipulação de DOM 🌳

Este repositório é dedicado ao estudo de como o JavaScript interage com o HTML e CSS através do **DOM (Document Object Model)**. Aqui, o foco é transformar páginas estáticas em aplicações interativas e dinâmicas.

## 🚀 O que estou aprendendo:

- **Seleção de Elementos**: Uso de `querySelector` e `getElementById`.
- **Event Listeners**: Captura de cliques, digitação e outros eventos de usuário.
- **Manipulação de Estilos**: Alteração de classes e CSS inline via JS.
- **Criação Dinâmica**: Adicionar e remover elementos HTML programaticamente.
- **Gestão de Estado**: Sincronizar os dados da aplicação com a interface.

## 💻 Projetos incluídos

- **Agenda de Contatos**: Aplicação que utiliza `Factory Functions` para gerir o estado dos contatos e reflete as mudanças no DOM.
- **Validadores de Formulário**: Lógica para feedback visual em tempo real.

## 📂 Organização

Seguindo o padrão de pastas que defini nos meus fundamentos:

- `js/`: Scripts de lógica e manipulação.
- `css/`: Estilização dos componentes dinâmicos.
- `index.html`: Estrutura base para os testes de DOM.

---

Desenvolvido por **Gustavo** 👨‍💻
